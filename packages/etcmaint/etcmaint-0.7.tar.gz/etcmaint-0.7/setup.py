#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['etcmaint', 'etcmaint.tests']

package_data = \
{'': ['*']}

install_requires = \
['zstandard']

entry_points = \
{'console_scripts': ['etcmaint = etcmaint.etcmaint:main']}

setup(name='etcmaint',
      version='0.7',
      description='An Arch Linux tool based on git for the maintenance of /etc files.',
      author='Xavier de Gaye',
      author_email='xdegaye@gmail.com',
      url='https://gitlab.com/xdegaye/etcmaint',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
