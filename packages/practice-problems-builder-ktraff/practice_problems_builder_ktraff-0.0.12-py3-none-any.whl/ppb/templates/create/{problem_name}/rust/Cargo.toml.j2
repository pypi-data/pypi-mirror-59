[package]
name = "{{ problem_name }}"
version = "0.1.0"
authors = ["{{ author_name }} <{{ author_email }}>"]
edition = "{{ rust_compiler_edition }}"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
