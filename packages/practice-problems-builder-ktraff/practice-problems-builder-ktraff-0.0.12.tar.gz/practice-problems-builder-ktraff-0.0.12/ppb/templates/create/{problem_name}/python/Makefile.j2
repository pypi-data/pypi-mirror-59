.PHONY: venv build test

venv_create:
	virtualenv -p {{ python_version }} venv

venv:
	. ./venv/bin/activate

build: venv
	pip install -r requirements.txt

test:
	PYTHONPATH=${PWD} pytest --log-cli-level=DEBUG tests