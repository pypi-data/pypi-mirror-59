from setuptools import setup

setup(name='dist_dist',
      version='0.0',
      description='Gaussian and Binomial distributions',
      packages=['dist_dist'],
      author_email='mihajlovic.aleksa@gmail.com',
      zip_safe=False)
