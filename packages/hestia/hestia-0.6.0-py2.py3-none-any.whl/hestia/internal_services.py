class InternalServices(object):  # noqa
    DOCKERIZER = 'dockerizer'
    INITIALIZER = 'initializer'
    SIDECAR = 'sidecar'
    HELPER = 'helper'
    RUNNER = 'runner'
    CONTROLLER = 'controller'

    VALUES = {
        DOCKERIZER,
        INITIALIZER,
        SIDECAR,
        HELPER,
        RUNNER,
        CONTROLLER
    }
