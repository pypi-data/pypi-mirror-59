
```note

规划：每周周日可以花时间来做这个项目{是的，只能花一天的时间！}，允许我花一年的时间开发完成！或一直开发下去也行！
下一步：搭建http文件系统服务和数据库服务（也就是两个没有前端代码的blueprint），编写前端通信js模块。
        要实现到这种效果：单页面应用能够直接连接已有的公共数据库，或创建私有的数据库（有独有的key），可以查询数据库的有无，
        要实现轻松的数据增删改查等操作；
再下一步：完善前端应用{文件资源浏览器和编辑器，增加保存和删除等功能}
再下一步：实现简易网盘应用
再下一步：实现并丰富单页面应用库，如md编辑器，图片查看器，笔记本，备忘录，自定义导航页，公告牌，文件分享等等应用。
再下一步：升级改善数据库，增加对可以建表的数据库的，增加对图片、视频等二进制文件的支持。
再下一步：blog应用
再下一步：高级网盘应用
后续：聊天应用？丰富前端GUI库（加入界面管理器？）？开发各种应单页面应用？丰富bp应用库？... 


所有js,html文件暂存templates目录下。后续将分割出js模块和html app 模块和通用templates模块
对于带有逻辑的apps，置于web.bps目录下，再加上一个web.web(主框架,App类)，web.utils(parse_json,user_manager模块)
再加上外部的piu(数据库)和pan(文件系统)的支持，就构成了完整的web模块。

bp(Blueprint)应用一般结构：py文件位于，web.bps.somebp目录下面，其他数据如js,html,css文件等，一律存放在data/bps/somebp目录下面
单页面应用：只能将html,css,js等数据存储在data/static/apps/someapp目录下面
js通用库模块存放于data/static/js目录下
templates通用模板存放于data/templates目录下
应用可以应用static下的公共模板和js库,也可以自己实现页面和逻辑至于自己的应用文件夹下。


综上，需要实现：

框架：
实现伪数据库，伪文件系统（伪数据库基于伪文件系统）
http框架实现登录模块
实现数据库、文件系统http服务
前后端通信模块{定义字段，提供操作函数，如定义请求和响应的格式}
前端GUI组件库(window...,.js 文件实现)
常用前端应用（在一个.js文件实现，文件资源浏览器，编辑器，md编辑器，图片查看器，To-do List）

前后端通信模块{
    #主要是要定义json响应的格式
    ==请求格式：
    cmd:命令
        op:操作类型，
        params:参数
    ==响应格式：
    success:操作成功与否
    msg:信息
    code:错误码
    data:数据
}

```

- 
- 数据层
`接口：类文件系统接口；`
- 文件系统
`本地文件系统，Github,文件系统服务器`
```

客户端：接口库，组件库
服务端：实现层，服务层


数据库、伪文件系统类: 可本地操作，或搭建为服务器
数据库、伪文件系统服务api：前端提供js版的通信库
前后端通信模块:定义请求方式和响应方式和相应的含义，并提供函数库
前端的通信后端数据库和文件系统的辅助库
前端界面组件库：window，界面管理器
前端标准应用程序库：explorer,editor
自定义应用程序
http框架层面：提供登录注册验证模块。提供提前参数解析服务
实例：
网盘系统：
    数据存储位置：本地文件系统，github或远程文件系统
    伪文件系统+伪数据库
    提供伪文件系统http服务（应用为文件系统、前后端通信、http框架、应用伪数据库（应用伪数据库）实例以存储配置信息，用户授权信息等）
    前后端通信模块
    前端伪文件系统操作库（应用前后端通信模块，在前端提供操作接口）
    GUI组件库(window)（提供方便的功能强大的GUI组件）
    网盘主界面+各种辅助应用(资源浏览器，编辑器，md编辑器，图片浏览器)
    网盘标准应用程序
    网盘运行实例
    
博客网站系统
    实现一：
    数据库
    服务端逻辑实现（调用数据库接口，调用登录注册模块，其余自己实现）
    前后端通信模块
    前端界面（调用GUI组件库，其余自我实现）
    实现二：
    数据库
    数据库服务
    前后端通信模块
    前端操作数据库
    前端直接实现应用程序（无需多余服务，不需要自己处理http请求）
分布式聊天系统
    ...
    实现服务器模块
    实现前端界面
论坛系统
    ...
    与博客系统类似

综上，需要实现：

框架：
实现伪数据库，伪文件系统（伪数据库基于伪文件系统）
http框架实现登录模块
实现数据库、文件系统http服务
前后端通信模块{定义字段，提供操作函数，如定义请求和响应的格式}
前端GUI组件库(window...,.js 文件实现)
常用前端应用（在一个.js文件实现，文件资源浏览器，编辑器，md编辑器，图片查看器，To-do List）

实现应用的两种方式：
1、后端只实现数据库、文件系统服务，其余应用逻辑由前端实现，可实现单文件应用！
2、后端实现应用逻辑，包括操作数据库、文件系统等

应用实例：

无后台应用！（例如：可以用单个的.html文件实现一个应用程序，因为只要有可用的文件系统或数据库http服务，你就可以直接把处理逻辑在前端实现，而后端只负责存储数据）

小应用：
自定义导航页，静态目录，公告牌，生成高速下载链接，简单笔记和to-do list

大应用：
网盘应用
博客应用
聊天应用
论坛应用
其他





```